# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
SELECT文を使ってtodosテーブル等の全レコードを全部取ってくる処理を行なうことができる

### Todoモデルのallメソッドの返り値は何か
Illuminate\Database\Eloquent\Collectionというクラスのインスタンスが返ってきます。

### 配列の代わりにCollectionクラスを使用するメリットは
CollectionインスタンスはLaravelで用意されているクラスで配列操作に特化したクラスになります。  
配列よりも便利で安全にデータ操作ができます。


### view関数の第1・第2引数の指定と何をしているか
Laravelでは、Controllerから、HTMLを表すbladeファイルへデータを渡すためにview関数を利用します。  
view関数は画面に表示したいbladeファイルを第一引数で指定し、第二引数に渡したいデータを連想配列の形で渡すことができます。  
view関数の第二引数の連想配列は、[blade内での変数名 => 代入したい値]を意味します。

### index.blade.phpの$todos・$todoに代入されているものは何か
$todoは１件分のtodoモデル. 
$todosはtodoモデルの集まり

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
フォームから送信された値を個別ではなく一括で取得するように変更します。  
Requestクラスの->all()を使用することで、フォームから送信された入力欄を連想配列で取得できる。

### fillメソッドは何をしているか
連想配列で取得した値を->fill()を使用して、Todoインスタンスの各プロパティに一括で代入します。  
->fill()は$todo->{連想配列のkey} = {連想配列のvalue}を配列の全ての要素に対して行ってくれます。  
Modelの->fill()を使用することで引数に指定した連想配列を一括代入できる。

### $fillableは何のために設定しているか
名前の通り->fill()によってModelに代入可能なプロパティを記述します。  
なぜこのような制限をかけるかというと、便利な一括代入には実は脆弱性があり、対策をせずに使用すると悪意のあるユーザに攻撃されてしまう恐れがあるためです。  
->fill()を使用した一括代入を行う際は、入力された値を信用せずにModelに$fillableを適切に設定し、意図しないカラムを更新されないように気をつけます。  
一括代入には脆弱性があるため$fillableを定義して代入できる項目に制限をかける必要があります。

### saveメソッドで実行しているSQLは何か
Todoインスタンスの->save()を実行してオブジェクトの状態をDBに保存するINSERT文を実行しています。

### redirect()->route()は何をしているか
指定したルート名をもとにURLを生成してそのURLへリダイレクト（画面遷移）している

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
SQLを知らなくても、PHPコードでテーブル操作ができるため学習コストが不要。  
マイグレーションファイルをGitで共有することで、開発者全員が同じテーブルを作成することができます。

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up()はphp artisan migrate を実行したときに呼ばれる。  
変更を適用するとき。  

down()はphp artisan migrate:rollback を実行したときに呼ばれる。  
変更を取り消すとき。

### Seederクラスの役割は何か
データベース（テーブルに対して）にあらかじめ用意した初期データやテストデータを自動的に挿入するための機能

### route関数の引数・返り値・使用するメリット
ルート名からそのルートで設定したURLを生成することができるのでURLを直接書かなくていい。  
route()はview()などと同じLaravelでのみ使用できるヘルパ関数と呼ばれる関数の一種です。

### @extends・@section・@yieldの関係性とbladeを分割するメリット
@extends('layouts.base')の記述で、継承する親Bladeを指定しています。  
引数はresources/views/以降のパスを.区切りで指定し、拡張子は省略できます。

そして、継承先の子Bladeの@section('content') ~ @endsectionで囲われた部分を、親Bladeの@yield('content')の部分に挿入しています。  
引数に同じ文字列'content'を指定することで、@section()と@yield()を紐づけています。

こうすることで、複数のBladeを組み合わせて1枚のHTMLを生成することが可能になります。  
重複するコードを共通化して再利用できるようになるため、保守性が向上しました。

### @csrfは何のための記述か
LaravelではCSRF攻撃を防ぐためのセキュリティ対策です。  
CSRF対策のためのトークンが含まれたinputタグが生成されているのを確認できます。 
この状態でフォームを送信することで、CSRF対策のトークンも一緒に送信され、Laravel側でトークンの検証も自動的に行ってくれます。

### {{ }}とは何の省略系か
Bladeの「echo（出力）」の省略記法です。  
値を安全に画面に表示する、HTMLとして解釈されないように文字を変換します。
